import streamlit as st
import requests
import json
import pandas as pd
import folium # 👈 เพิ่มบรรทัดนี้
from streamlit_folium import st_folium # 👈 เพิ่มบรรทัดนี้
# --- 1. ตั้งค่าหน้าเว็บ ---
st.set_page_config(page_title="Sensor Team Dashboard", page_icon="⚙️", layout="wide")

# --- 2. ดึงกุญแจลับจากตู้เซฟ ---
GAS_URL = st.secrets["GAS_URL"]
SHEET_URL = st.secrets["SHEET_URL"]

# --- 3. สร้างระบบเมนูแถบด้านข้าง (Sidebar) ---
st.sidebar.title("🛠️ Sensor Team Menu")
menu = st.sidebar.radio("เลือกเมนูการใช้งาน:", [
    "📍 1. ภาพรวมและแผนที่ (Monthly Overview)",
    "🏢 2. เจาะลึกรายไซต์ (Site Detail)",
    "📱 3. กระดานงานส่วนตัว (My Workload)",
    "📊 4. ภาพรวมงานของทีม (Team Manager)",
    "🧰 5. ระบบเบิก-คืนอุปกรณ์ (Tools)",
    "👥 6. ข้อมูลทีม (Team Profile)",
    "🧠 7. ศูนย์การเรียนรู้ (Learning & Quiz)",
    "📚 8. คู่มือการใช้งาน (Manuals & Docs)"
])

st.sidebar.markdown("---")
st.sidebar.info("👨‍💻 เข้าสู่ระบบโดย: **Heart**")

# --- 4. โครงสร้างแต่ละเมนู ---
if menu == "📍 1. ภาพรวมและแผนที่ (Monthly Overview)":
    st.title("📍 แดชบอร์ดประจำเดือน และ แผนที่ไซต์งาน")
    
    # --- ฟังก์ชันดึงข้อมูลจาก GSheet แบบอ่านแยกแผ่น (Tabs) ---
    @st.cache_data(ttl=60) # โหลดซ้ำทุก 60 วินาที
    def load_sheet(sheet_name):
        # ดึง ID ของ GSheet จากลิงก์ในตู้เซฟ
        sheet_id = SHEET_URL.split("/d/")[1].split("/")[0]
        # ใช้เวทมนตร์ Google API ดึงข้อมูลเป็น CSV ตามชื่อแผ่น
        csv_url = f"https://docs.google.com/spreadsheets/d/{sheet_id}/gviz/tq?tqx=out:csv&sheet={sheet_name}"
        return pd.read_csv(csv_url)

    with st.spinner("กำลังโหลดข้อมูลแผนที่และฐานข้อมูล..."):
        try:
            # 1. โหลดข้อมูลจากแผ่น Master_Site
            df_master = load_sheet("Master_Site")
            
            # ล้างข้อมูลแถวที่ว่างเปล่า หรือแถวที่ยังไม่ได้ใส่พิกัด Lat, Long
            df_map = df_master.dropna(subset=['ละติจูด (Latitude)', 'ลองจิจูด (Longitude)'])
            
            col1, col2 = st.columns([2, 1]) # แบ่งหน้าจอเป็น 2 ส่วน (แผนที่ใหญ่กว่าตาราง)
            
            with col1:
                st.markdown("### 🗺️ แผนที่พิกัดไซต์งาน")
                # สร้างแผนที่ เริ่มต้นจุดศูนย์กลางที่ประเทศไทย (Lat 13.7, Lon 100.5)
                m = folium.Map(location=[13.736717, 100.523186], zoom_start=6)
                
                # วนลูปอ่านข้อมูลทุกแถวเพื่อปักหมุด
                for index, row in df_map.iterrows():
                    site_name = row['ชื่อไซต์งาน (Process Work)']
                    lat = row['ละติจูด (Latitude)']
                    lon = row['ลองจิจูด (Longitude)']
                    county = row.get('กลุ่มงาน (County)', 'ไม่ระบุ')
                    
                    # ข้อความ Pop-up เวลากดที่หมุด
                    popup_text = f"<b>{site_name}</b><br>กลุ่มงาน: {county}"
                    
                    # ปักหมุดลงแผนที่ (ตอนนี้ให้เป็นสีเขียวทั้งหมดก่อน เดี๋ยวเรามาทำเงื่อนไขสีแดง/เหลืองทีหลัง)
                    folium.Marker(
                        [lat, lon], 
                        popup=popup_text,
                        icon=folium.Icon(color="green", icon="info-sign")
                    ).add_to(m)
                
                # นำแผนที่มาโชว์บนหน้าเว็บ Streamlit
                st_folium(m, width=700, height=500)
                
            with col2:
                st.markdown("### 📋 สรุปฐานข้อมูลไซต์")
                st.metric("จำนวนไซต์งานทั้งหมด", f"{len(df_master)} ไซต์")
                st.metric("พร้อมปักหมุดบนแผนที่", f"{len(df_map)} ไซต์")
                
                st.markdown("**รายชื่อไซต์งาน:**")
                # โชว์ตารางแบบลบ Index (ตัวเลขด้านหน้า) ออกให้ดูสะอาด
                st.dataframe(df_master[['กลุ่มงาน (County)', 'ชื่อไซต์งาน (Process Work)']], hide_index=True)
                
        except Exception as e:
            st.error(f"เกิดข้อผิดพลาดในการดึงข้อมูล: {e}")
            st.info("💡 รบกวนคุณ Heart ตรวจสอบว่าแผ่น 'Master_Site' ใน GSheet สะกดถูกต้องไหมครับ")

# --- โค้ดเมนูอื่นๆ ที่อยู่ด้านล่าง ปล่อยไว้เหมือนเดิมครับ ---

elif menu == "📱 3. กระดานงานส่วนตัว (My Workload)":
    st.title("📱 กระดานงานส่วนตัว")
    st.write("หน้าต่างสำหรับดูงานของตัวเอง และแจ้งงานด่วน")
    
    # ดึงรายชื่อทีมงานทั้งหมด
    team_members = ["Heart", "Phubeth", "Mink", "Film", "Folk", "Chan"]
    
    st.markdown("### ➕ ฟอร์มแจ้งงานด่วน / อัปเดตงาน")
    with st.form("task_form"):
        col1, col2 = st.columns(2)
        
        with col1:
            site_name = st.text_input("ชื่อไซต์งาน", placeholder="เช่น CPN อยุธยา")
            task_detail = st.text_input("ชื่องาน / รายละเอียด", placeholder="เช่น เข้าไปเปลี่ยนซิมเร้าเตอร์")
            task_type = st.selectbox("ประเภทงาน", ["งานด่วน", "งานตามแพลน", "งานโปรเจกต์"])
            status = st.selectbox("สถานะงาน", ["Planning", "In progress", "Problem", "Complete"])
            
        with col2:
            start_date = st.date_input("วันที่เข้าทำ (Scheduled Date)")
            end_date = st.date_input("กำหนดเสร็จ (Deadline)")
            assignee = st.selectbox("ผู้รับผิดชอบหลัก", team_members)
            # เพิ่มช่องเลือกผู้ช่วย (เลือกได้หลายคน)
            assistants = st.multiselect("ผู้ช่วย (ถ้ามี)", team_members)
            
        submitted = st.form_submit_button("บันทึกข้อมูลลงตาราง", type="primary")
        
        if submitted:
            if site_name and task_detail:
                # แปลงรายชื่อผู้ช่วยให้เป็นข้อความคั่นด้วยลูกน้ำ เช่น "Base, Mink"
                assistants_str = ", ".join(assistants)
                
                # เตรียมข้อมูลให้ตรงกับ 9 คอลัมน์ใน GSheet แผ่น Task & Workload
                payload = {
                    "sheet": "Task & Workload",
                    "data": [
                        (pd.Timestamp.utcnow() + pd.Timedelta(hours=7)).strftime("%Y-%m-%d %H:%M:%S"), # A: วันที่บันทึก (เวลาไทย +7) # A: วันที่บันทึก
                        site_name,                                        # B: ชื่อไซต์งาน
                        task_detail,                                      # C: รายละเอียด
                        task_type,                                        # D: ประเภทงาน
                        start_date.strftime("%d/%m/%Y"),                  # E: วันที่เข้าทำ
                        end_date.strftime("%d/%m/%Y"),                    # F: กำหนดเสร็จ
                        status,                                           # G: สถานะงาน
                        assignee,                                         # H: ผู้รับผิดชอบหลัก
                        assistants_str                                    # I: ผู้ช่วย
                    ]
                }
                
                with st.spinner("กำลังส่งข้อมูลเข้าตาราง..."):
                    try:
                        response = requests.post(GAS_URL, data=json.dumps(payload))
                        result = response.json()
                        
                        # เช็คว่า Google Script ตอบกลับมาว่า success จริงๆ ไหม
                        if result.get("status") == "success":
                            st.success(f"บันทึกงาน '{task_detail}' ที่ '{site_name}' ให้คุณ {assignee} เรียบร้อยแล้ว! 🎉")
                        else:
                            st.error(f"เกิดข้อผิดพลาดจากฐานข้อมูล: {result.get('message')}")
                    except Exception as e:
                        st.error(f"ระบบขัดข้อง: {e}")
            else:
                st.warning("⚠️ กรุณากรอก 'ชื่อไซต์งาน' และ 'รายละเอียดงาน' ให้ครบถ้วนครับ")

else:
    st.title(menu)
    st.write(f"กำลังพัฒนาฟีเจอร์สำหรับเมนูนี้ครับ...")
