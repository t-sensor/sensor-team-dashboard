import streamlit as st
import requests
import json
import pandas as pd

# --- 1. ตั้งค่าหน้าเว็บ ---
st.set_page_config(page_title="Sensor Team Dashboard", page_icon="⚙️", layout="wide")

# --- 2. ดึงกุญแจลับจากตู้เซฟ ---
GAS_URL = st.secrets["GAS_URL"]
SHEET_URL = st.secrets["SHEET_URL"]

# --- 3. สร้างระบบเมนูแถบด้านข้าง (Sidebar) ---
st.sidebar.title("🛠️ Sensor Team Menu")
menu = st.sidebar.radio("เลือกเมนูการใช้งาน:", [
    "📍 1. ภาพรวมและแผนที่ (Monthly Overview)",
    "🏢 2. เจาะลึกรายไซต์ (Site Detail)",
    "📱 3. กระดานงานส่วนตัว (My Workload)",
    "📊 4. ภาพรวมงานของทีม (Team Manager)",
    "🧰 5. ระบบเบิก-คืนอุปกรณ์ (Tools)",
    "👥 6. ข้อมูลทีม (Team Profile)",
    "🧠 7. ศูนย์การเรียนรู้ (Learning & Quiz)",
    "📚 8. คู่มือการใช้งาน (Manuals & Docs)"
])

st.sidebar.markdown("---")
st.sidebar.info("👨‍💻 เข้าสู่ระบบโดย: **Heart**")

# --- 4. โครงสร้างแต่ละเมนู ---
if menu == "📍 1. ภาพรวมและแผนที่ (Monthly Overview)":
    st.title("📍 แดชบอร์ดประจำเดือน (Monthly Overview)")
    st.write("กำลังพัฒนา: ระบบจะดึงแผนที่และไซต์ที่ต้อง PM ในเดือนนี้มาแสดงที่นี่")

elif menu == "📱 3. กระดานงานส่วนตัว (My Workload)":
    st.title("📱 กระดานงานส่วนตัว")
    st.write("หน้าต่างสำหรับดูงานของตัวเอง และแจ้งงานด่วน")
    
    # 🌟 ส่วนทดสอบระบบยิง API (เขียนข้อมูลลง GSheet)
    st.markdown("### ➕ ฟอร์มแจ้งงานด่วน (ทดสอบระบบ API)")
    with st.form("task_form"):
        site_name = st.text_input("ชื่อไซต์งาน", placeholder="เช่น CPN อยุธยา")
        task_detail = st.text_input("รายละเอียดงาน", placeholder="เช่น เข้าไปเช็คเซ็นเซอร์ด่วน")
        assignee = st.selectbox("ผู้รับผิดชอบหลัก", ["Heart", "Phubeth", "Mink", "Film", "Folk", "Chan"])
        
        submitted = st.form_submit_button("บันทึกข้อมูล")
        
        if submitted:
            if site_name and task_detail:
                # สร้างชุดข้อมูลเพื่อส่งไปให้ Google Apps Script
                payload = {
                    "sheet": "Task & Workload", # ต้องพิมพ์ให้ตรงกับชื่อ Tab ใน GSheet เป๊ะๆ
                    "data": [pd.Timestamp.now().strftime("%Y-%m-%d %H:%M:%S"), site_name, task_detail, "งานด่วน", "", "", "รอดำเนินการ", assignee, ""]
                }
                
                # สั่งยิงข้อมูล
                with st.spinner("กำลังบันทึกข้อมูลลงตาราง..."):
                    try:
                        response = requests.post(GAS_URL, data=json.dumps(payload))
                        if response.status_code == 200:
                            st.success(f"บันทึกงานด่วนที่ {site_name} ให้ {assignee} เรียบร้อยแล้ว! 🎉")
                    except Exception as e:
                        st.error(f"เกิดข้อผิดพลาด: {e}")
            else:
                st.warning("กรุณากรอกชื่อไซต์และรายละเอียดให้ครบถ้วนครับ")

else:
    st.title(menu)
    st.write(f"กำลังพัฒนาฟีเจอร์สำหรับเมนูนี้ครับ...")
